# frozen_string_literal: true

source "https://rubygems.org"

# The gem's own dependencies. OpenLoam has no development dependencies of its own —
# the harness drives real Rails apps through `gem install`ed binaries in an
# unbundled environment, deliberately, so it does not want them on this bundle.
gemspec
