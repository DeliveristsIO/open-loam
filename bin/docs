#!/usr/bin/env bash
# Preview the Jekyll docs site locally (http://localhost:4000/open-loam/).
set -euo pipefail

cd "$(dirname "$0")/../docs"
bundle check >/dev/null 2>&1 || bundle install
exec bundle exec jekyll serve --livereload
